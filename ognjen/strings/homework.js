const firstName = "ognjen"
const lastName = "dokic"

const firstNameCapital = firstName[0].toUpperCase() + firstName.substring(1)
const lastNameCapital = lastName[0].toUpperCase() + lastName.substring(1)

console.log('full name: ', firstNameCapital + ' ' + lastNameCapital);